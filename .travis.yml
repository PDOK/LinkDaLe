language: node_js
node_js:
  - "7"
cache:
  directories:
    - node_modules
script:
  - npm test
  - npm run build
deploy:
  provider: script
  local_dir: build
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master
